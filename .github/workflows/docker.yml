name: Docker Build

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - '*.md'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          load: true
          tags: forever-jiang:latest
          build-args: |
            DOCKER_HUB_USERNAME=foreverjiang

      - name: Export Docker image
        run: |
          docker save forever-jiang:latest | gzip > docker-image.tar.gz
          ls -lh docker-image.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: docker-image.tar.gz
          retention-days: 7

  summary:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Build Summary
        run: |
          echo "## Docker镜像构建完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ 构建成功" >> $GITHUB_STEP_SUMMARY
          echo "📦 镜像: forever-jiang:latest" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "💾 镜像已保存为artifact，可下载使用" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 下载并使用镜像" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "gunzip docker-image.tar.gz" >> $GITHUB_STEP_SUMMARY
          echo "docker load < docker-image" >> $GITHUB_STEP_SUMMARY
          echo "docker run -p 3000:3000 -e JWT_SECRET='your-secret' forever-jiang:latest" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
